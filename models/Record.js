const mongoose = require('mongoose');

const RecordSchema = mongoose.Schema({
    name: {
        type: String,
        unique: true,
        required: yes
    },
    releaseDate: {
        type: 
    }
})